export const metadata = {
    title: "Habibis|Catering",
    description: "This this is the catering page",
  };
  export default function Layout({ children }) {
    return (
        <section>
            {children}
        </section>
    );
  }
