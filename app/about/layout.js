export const metadata = {
    title: "Habibis|About",
    description: "This this is the about page",
  };
  export default function Layout({ children }) {
    return (
        <section>
            {children}
        </section>
    );
  }



